version: '3.8'

services:
  badminton-backend:
    build: .
    container_name: badminton-backend
    ports:
      - "3000:3000"
    volumes:
      # Mount the database directory with absolute path to persist data across deployments
      - /home/ashok/badminton-database:/app/database
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3000/status"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
